APP_BASE_URL=http://localhost:3000

# Database (POC/defaults)
DB_HOST=localhost
DB_PORT=5433
DB_USER=postgres
DB_PASS=postgres
DB_NAME=app

# JWT
JWT_ACCESS_SECRET=change_me_access_secret
JWT_REFRESH_SECRET=change_me_refresh_secret
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d

# Auth (magic link)
MAGIC_TOKEN_TTL_MIN=15

# Note:
# - Do NOT commit real secrets. Copy this file to `.env` and adjust locally.
# - Rotate any leaked secrets immediately.
